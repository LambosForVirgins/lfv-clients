import { Button } from './Button';
import { BlockLayout, InlineLayout } from '../Layouts'
import { SideBySide, ExampleWrapper, PunchCard, Backdrop, SkeletonText, SkeletonTextBlock, Card } from '../../shared'

# Button

The Button component is a versatile, accessible button element that supports various styles and states. Our 8-bit button is a nostalgic nod to arcade days, pixel-perfect for game interfaces that want to scream *"Press Start!"* with attitude. It’s loud, blocky, and makes no apologies. Whether you're building a player dashboard, a giveaway trigger, or a minting interaction — this button gets clicked.

#### Everything is a button

In principle, try to think of the most actionable elements as buttons. Thinking and building this way ensures we have the right touch target areas and user feedback. It also makes our experiences easier to localize and friendlier for people using screen readers.

## Principles

- **Signal interactivity**: It should *look* like it does something.
- **Feel authentic**: This isn’t a CSS filter on a modern button — it's crafted to *look and behave* like it belongs on a Game Boy screen.
- **Boost user delight**: It's not just retro, it’s fun. Every click should feel like a coin drop.

### Actionable

A button label indicates what happens when the user taps the button, even if it's just to acknowledge something.

### Contextual

Buttons work with other elements on a screen to surface the most important actions the user wants to take in that context. 

### Incisive

Buttons capture user intent concisely, distilling the action a user wants to take into an inclusive word or phrase. 

## Anatomy 

The most basic setup of a button includes only a single label or icon. Still, a button can also be customized to include a label with a leading icon and a trailing icon. Circle buttons can also support an additional label below the button's container.

### Shape

Buttons come in rectangular, circle, square, and pill shapes.

#### Rectangular
Rectangular buttons are typically leveraged at fixed widths, 100% and 50% width on mobile, and a variety of widths on the web. Large rectangle buttons are commonly used CTA buttons that move users forward in a flow.

#### Circle
Circular buttons can include a text label or an icon. An optional label below is available as well. All maintain a center position.

#### Square
Square buttons, similar to circle buttons, can include a text label or an icon. All maintain a center position.

#### Pill
Pill buttons are more commonly in line with other content and controls.


## Usage

Use buttons to let users take action, make choices, and move forward. Buttons can navigate to other pages while completing an action like purchasing or submitting a form.

```jsx
import { Button } from '@lfv/elements/Buttons/Button';

<Button>
  Click me
</Button>
```

### Stuff

## Hierarchy

Button hierarchy emphasizes which button is more important in the context so the user can take action immediately. 

### Primary Button

Think of it as the big red button in your interface design: obvious, satisfying, and too tempting not to hit.

Use one primary button per context to allow users to take a specific action, move forward in a flow, acknowledge and dismiss, or finish a task.

<SideBySide>
  <ExampleWrapper title="Do" message={`Use one primary button per context to highlight the action the user will most likely take.`}>
    <Backdrop shade='green'>
      <Card padded elevated>
        <BlockLayout>
          <SkeletonTextBlock />
          <Button variant="secondary">Secondary</Button>
          <Button variant="primary">Primary</Button>
          <Button variant="tertiary">Tertiary</Button>
        </BlockLayout>
      </Card>
    </Backdrop>
  </ExampleWrapper>
  <ExampleWrapper title="Don't" message={`Avoid using multiple primary buttons on a screen.`}>
    <Backdrop shade='red'>
      <Card padded elevated>
        <BlockLayout>
          <SkeletonTextBlock />
          <Button variant="primary">Primary</Button>
          <Button variant="primary">Primary</Button>
          <Button variant="tertiary">Tertiary</Button>
        </BlockLayout>
      </Card>
    </Backdrop>
  </ExampleWrapper>
</SideBySide>

### Secondary Button

Secondary buttons are the bread and butter of the button world. Lean into them whenever possible. The gray background protection provides higher visibility, making them more accessible than tertiary buttons. 

<SideBySide>
  <ExampleWrapper title="Do" message={`Use secondary buttons for most icon buttons since the grey background protection makes the button more visible and accessible.`}>
    <Backdrop shade='green'>
      <Card padded elevated>
        <InlineLayout>
          <SkeletonText/>
          <Button variant="secondary">S</Button>
          <Button variant="secondary">S</Button>
          <Button variant="secondary">S</Button>
        </InlineLayout>
      </Card>
    </Backdrop>
  </ExampleWrapper>
  <ExampleWrapper title="Don't" message={`Don't use icons without background protection as buttons. A single icon may be used to indicate a status (think warning icon) or to provide affordance for tapping the entire row (disclosure icon or accordion icon).`}>
    <Backdrop shade='red'>
      <Card padded elevated>
        <InlineLayout>
          <SkeletonText/>
          <Button variant="tertiary">T</Button>
          <Button variant="tertiary">T</Button>
          <Button variant="tertiary">T</Button>
        </InlineLayout>
      </Card>
    </Backdrop>
  </ExampleWrapper>
</SideBySide>

### Accent Button

<Button variant="accent">
  Accent Button
</Button>

### Tertiary

Tertiary buttons are mainly used for dismissive actions that give users a way out of something, letting them do nothing, dismiss, or skip.

<Button variant="tertiary">
  Tertiary Button
</Button>

## Sizing

<Backdrop>
  <InlineLayout>
    <Button variant="primary" size="small">Small</Button>
    <Button variant="primary" size="medium">Medium</Button>
    <Button variant="primary" size="large">Large</Button>
  </InlineLayout>
</Backdrop>

## States

### Disabled

Disabled buttons can be confusing and frustrating for many users and can be difficult to spot for those with low vision. To make things easier, try to enable the button so users can tap it, and provide an error message if any information is missing or incorrect. If you must use a disabled button, include clarifying text, an error message, or a tooltip to explain why. That way, everyone can easily understand why the button is disabled.

<Backdrop>
  <div style={{ display: 'flex', flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 'var(--size-300)', padding: 'var(--size-500)', background: '#fff', borderRadius: 'var(--size-100)', boxShadow: 'rgba(0, 0, 0, 0.02) 0px 2px 1px, rgba(0, 0, 0, 0.04) 0px 4px 2px, rgba(0, 0, 0, 0.05) 0px 8px 4px, rgba(0, 0, 0, 0.06) 0px 16px 8px, rgba(0, 0, 0, 0.08) 0px 32px 16px' }}>
    <Button variant="primary" disabled>Disabled</Button>
  </div>
</Backdrop>

### Loading

<Backdrop>
  <div style={{ display: 'flex', flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 'var(--size-300)', padding: 'var(--size-500)', background: '#fff', borderRadius: 'var(--size-100)', boxShadow: 'rgba(0, 0, 0, 0.02) 0px 2px 1px, rgba(0, 0, 0, 0.04) 0px 4px 2px, rgba(0, 0, 0, 0.05) 0px 8px 4px, rgba(0, 0, 0, 0.06) 0px 16px 8px, rgba(0, 0, 0, 0.08) 0px 32px 16px' }}>
    <Button variant="primary" loading>Loading</Button>
  </div>
</Backdrop>

## Accessibility

The Button component follows accessibility best practices:

- Uses semantic HTML (`<button>` element)
- Supports keyboard navigation
- Includes proper ARIA attributes
- Provides focus indicators

## Design Tokens

The Button component uses the following design tokens: